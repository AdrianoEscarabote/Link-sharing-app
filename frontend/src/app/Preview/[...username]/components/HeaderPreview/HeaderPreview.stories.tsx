import { Meta, StoryObj } from "@storybook/react";
import HeaderPreview from ".";

export default {
  title: "app/Preview/Header",
  component: HeaderPreview,
  tags: ["autodocs"],
} as Meta;

export const Primary: StoryObj = {};
