import { Meta, StoryObj } from "@storybook/react";

import Header from ".";

export default {
  title: "components/Header",
  component: Header,
  tags: ["autodocs"],
} as Meta;

export const Primary: StoryObj = {};
